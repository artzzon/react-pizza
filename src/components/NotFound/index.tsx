import styles from "./NotFound.module.scss";

const NotFound: React.FC = () => {
  // доделать стили, кнопку "вернуться на главную"
  return <h1 className={styles.root}>Страница не найдена</h1>;
};

export default NotFound;
